{
  "name": "fluxoapp-push-server",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute fluxo_push --remote --file=schema.sql"
  },
  "dependencies": {
    "@mmmike/web-push": "1.3.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}